<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ssafy.happyhouse.model.dao.CrimeDao">

	<select id="getCrime" resultType="CrimeDto">
		select * from crime where siname = #{siname}
	</select>
	
	<select id="getCrimeList" resultType="CrimeDto">
		select * from crime
	</select>

</mapper>